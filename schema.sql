create table components(
	id integer primary key,
	PCpart varchar,
	brand varchar,
	price integer
);

insert into components values(1, 'CPU', 'Asus', 200);
insert into components values(2, 'CPU', 'msi', 120);
